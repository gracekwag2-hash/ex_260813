# 구글 검색에 우리 사이트 등록하기

"프로포즈 게임"으로 검색했을 때 사이트가 나오게 하는 작업입니다.

---

## 먼저 알아두실 점

검색 등록과 검색 순위는 **다른 이야기**입니다.

| 구분 | 걸리는 시간 | 가능성 |
|---|---|---|
| 구글에 색인되기 (검색되긴 함) | 3일 ~ 2주 | 거의 확실 |
| `프로포즈 게임 나와 데이트할래` 같은 긴 검색어로 노출 | 2주 ~ 1개월 | 높음 |
| **`프로포즈 게임` 단독 검색어 첫 페이지** | 수개월 이상 | 낮음 |

`프로포즈 게임`은 이미 경쟁 사이트가 많은 검색어예요. 새로 만든 사이트가
바로 올라가긴 어렵습니다. 아래 작업은 **출발선에 서는 것**까지는 확실히 해주고,
순위는 방문자가 쌓이면서 서서히 올라갑니다.

순위에 가장 큰 영향을 주는 건 아래 두 가지입니다.

- **직접 도메인** — `xxx.vercel.app` 같은 공유 도메인은 구글이 낮게 평가합니다.
  `.com` 도메인을 사면(연 1~2만원) 효과가 꽤 큽니다.
- **다른 곳에서 걸어준 링크** — 블로그, 커뮤니티, SNS에 링크가 퍼질수록 올라갑니다.

---

## 이미 해둔 것 (코드 작업)

제가 아래를 미리 넣어뒀습니다. `git_push.bat` 만 실행하면 반영돼요.

- 검색용 제목: `프로포즈 게임 | 나와 데이트할래? 💕 도망가는 싫어 버튼`
- 검색결과에 나올 설명문(description)
- 카카오톡·페이스북 공유 미리보기 정보
- 구글이 서비스 종류를 이해하도록 돕는 구조화 데이터
- `robots.txt` (크롤러 허용)
- `sitemap.xml` (페이지 목록)

> 링크를 받은 상대방 화면에서는 탭 제목이 `혜원아, 나와 데이트할래? 💕` 로
> 자동으로 바뀝니다. 검색용 제목과 따로 놀게 만들어뒀어요.

---

## 1단계 · 서치 콘솔에 사이트 등록

1. [search.google.com/search-console](https://search.google.com/search-console) 접속
2. 구글 계정으로 로그인
3. 왼쪽 위 **속성 추가** 클릭
4. 두 가지 중 **오른쪽 `URL 접두어`** 를 선택합니다
   (왼쪽 `도메인`은 DNS 설정이 필요해서 vercel.app 주소로는 불가능해요)
5. 입력칸에 아래 주소를 붙여넣고 **계속**

```
https://ex-260813-awjy.vercel.app/
```

---

## 2단계 · 소유권 확인

여러 방법이 나오는데 **`HTML 파일` 방법이 제일 쉽습니다.**

1. **HTML 파일** 항목을 펼치고 **파일 다운로드** 클릭
2. `google1a2b3c4d5e6f.html` 같은 파일이 받아집니다
3. 그 파일을 **`26-2` 폴더에 그대로 복사**
4. **`git_push.bat`** 더블클릭 → 1분 대기
5. 서치 콘솔로 돌아와 **확인** 클릭

`소유권이 확인되었습니다` 가 뜨면 성공입니다.

<details>
<summary>HTML 태그 방법으로 하고 싶다면</summary>

`index.html` 을 메모장으로 열고 `google-site-verification` 을 찾으세요.
설명 주석이 끝나는 줄 바로 **아래**에 서치 콘솔에서 받은 태그를 한 줄 붙여넣습니다.

```html
<meta name="google-site-verification" content="서치콘솔에서_받은_코드">
```

저장 후 `git_push.bat` 실행하고 **확인** 을 누르세요.

> 주의: 붙여넣을 때 주석 기호 안쪽에 넣으면 안 됩니다. 주석이 끝난 다음 줄에 넣으세요.
</details>

---

## 3단계 · 사이트맵 제출

1. 서치 콘솔 왼쪽 메뉴 **Sitemaps** 클릭
2. 입력칸에 `sitemap.xml` 만 입력 (전체 주소 아님)
3. **제출** 클릭
4. 상태가 `성공` 으로 바뀌면 완료

---

## 4단계 · 색인 요청 (제일 중요)

가만히 두면 구글이 찾아올 때까지 몇 주 걸립니다. 직접 요청하면 훨씬 빨라져요.

1. 서치 콘솔 맨 위 **검색창**에 아래 주소를 붙여넣고 엔터

```
https://ex-260813-awjy.vercel.app/
```

2. `URL이 Google에 등록되어 있지 않음` 이라고 나올 거예요 (정상입니다)
3. **색인 생성 요청** 클릭
4. 1~2분 기다리면 `색인 생성을 요청함` 으로 바뀝니다

며칠 뒤 구글에서 `site:ex-260813-awjy.vercel.app` 로 검색해보세요.
결과가 나오면 색인이 완료된 겁니다.

---

## 순위를 올리고 싶다면

**효과 큰 순서대로**

1. **직접 도메인 사기** — 가비아·후이즈 등에서 연 1~2만원.
   Vercel > Settings > Domains 에서 연결하면 됩니다. 서치 콘솔은 새 주소로 다시 등록해야 해요.
2. **링크 퍼뜨리기** — 인스타, 커뮤니티, 블로그에 소개글과 함께 링크를 올리세요.
   실제 사용자가 링크를 타고 들어오는 게 순위에 제일 크게 작용합니다.
3. **설명 콘텐츠 추가** — 지금은 게임 화면뿐이라 구글이 읽을 글이 거의 없어요.
   "프로포즈 게임 사용법" 같은 소개 문단을 페이지 아래에 넣으면 도움이 됩니다.
4. **공유 미리보기 이미지** — 카톡으로 링크를 보낼 때 뜨는 썸네일이 아직 없습니다.
   이미지를 만들어 넣으면 클릭률이 올라가요.

3번과 4번은 원하시면 만들어 드릴 수 있어요.

---

## 확인용 주소

배포 후 아래 두 개가 정상적으로 열리는지 확인해보세요.

- `https://ex-260813-awjy.vercel.app/robots.txt`
- `https://ex-260813-awjy.vercel.app/sitemap.xml`
